"""Pydantic request/response schemas.

The previous finance-note / watchlist / ingest schemas were removed in the
schema reset. This module is intentionally empty — define new Pydantic models
here as endpoints are added.
"""
